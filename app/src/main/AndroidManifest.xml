<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fadal17068266">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudModListAcitvity"></activity>
        <activity android:name=".ModuleClasses.UpdateModuleActivity" />
        <activity android:name=".ModuleClasses.AddModuleActivity" />
        <activity android:name=".ModuleClasses.ModuleListActivity" />
        <activity android:name=".StudentClasses.UploadImageActivity" />
        <activity android:name=".StudentClasses.UpdateStudentActivity" />
        <activity android:name=".StudentClasses.AddStudentActivity" />
        <activity android:name=".StudentClasses.StudentListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>